name: 'marketing_warehouse'
version: '1.0.0'
config-version: 2

profile: 'marketing_warehouse'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
docs-paths: ["docs"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  marketing_warehouse:
    staging:
      +materialized: view
    marts:
      +materialized: table

seeds:
  marketing_warehouse:
    +quote_columns: false
